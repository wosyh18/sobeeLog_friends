//userid를 통신을 통해 가져올 수 있게. 
const http = require("http");
const { friendsDB } = require("../../models");

module.exports = async(req, res) => {
    
    try{
        



    }

    catch(err){
        console.log(err);
    }

}